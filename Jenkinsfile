pipeline {
    agent any

    stages {

        stage('Deploy') {
            steps {
                sh '''
                echo "Hello Jenkins"
                pwd
                ls -la
                '''
            }
        }
    }
}